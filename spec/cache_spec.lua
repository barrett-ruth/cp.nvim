---@diagnostic disable: undefined-global, undefined-field
describe('cache commands', function()
  it(':CP cache read pretty-prints cache and quits with q', function()
    pending('open read view; simulate q; ensure buffer closes')
  end)

  it(':CP cache clear [contest] prunes cache and invalidates state', function()
    pending('clear subset and all; verify files removed and state updated')
  end)
end)

