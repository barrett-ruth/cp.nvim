---@diagnostic disable: undefined-global, undefined-field
describe('state restoration (:CP with no args)', function()
  it('restores platform/contest/problem/language from current file', function()
    pending('open a problem source; call :CP; assert restored state')
  end)
end)

