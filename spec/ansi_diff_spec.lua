---@diagnostic disable: undefined-global, undefined-field
describe('ANSI and diff integration', function()
  it('applies ANSI highlight groups from program/compiler output', function()
    pending('emit output with ANSI; inspect extmarks/highlights or groups present')
  end)

  it('cycles diff modes none → git → vim', function()
    pending('simulate keypress or call API; assert mode changes and view updates')
  end)
end)
