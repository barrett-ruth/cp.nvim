---@diagnostic disable: undefined-global, undefined-field
describe('picker integration (:CP pick)', function()
  it('dispatches to configured picker (telescope/fzf-lua)', function()
    pending('configure picker in opts; intercept call to picker module')
  end)

  it('<c-r> forces refresh/update of contest list', function()
    pending('simulate refresh trigger and assert data source reloaded')
  end)

  it('behaves gracefully when picker = nil (no picker)', function()
    pending('ensure no crash; maybe echo guidance')
  end)
end)

