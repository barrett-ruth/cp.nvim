---@diagnostic disable: undefined-global, undefined-field
describe('interactive mode (:CP interact)', function()
  it('opens terminal and runs current program', function()
    pending('assert a terminal buffer opens; process object exists')
  end)

  it('orchestrates program with executable interactor', function()
    pending('provide dummy interactor; assert both processes wired; <c-q> restores layout')
  end)
end)
