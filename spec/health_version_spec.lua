---@diagnostic disable: undefined-global, undefined-field
describe('version and health', function()
  it('exposes version string from cp.version', function()
    pending('require cp.version and assert semver-ish pattern')
  end)

  it('health checker validates external tools (uv, git)', function()
    pending('run health entry; assert lines for required tools present')
  end)
end)
